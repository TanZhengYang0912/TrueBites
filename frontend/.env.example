# Google Maps — browser key for rendering the map
# Get from: Google Cloud Console → APIs & Services → Credentials → Create API Key
# Enable: Maps JavaScript API
VITE_MAPS_BROWSER_KEY=your_google_maps_browser_key_here

# Backend URL — where the Node.js server is running (don't change this).
# AI processing (video download, transcription, extraction) is served from
# this same backend under /api/ai — no separate service or URL needed.
VITE_API_BASE=http://localhost:4000

# Map ID for the custom-styled vector map (Advanced Markers). Create one in
# Google Cloud Console → Map Management, in the SAME project as the key above.
# Use DEMO_MAP_ID to run without a custom style.
VITE_MAP_ID=DEMO_MAP_ID

# Supabase — get from Supabase Dashboard → Settings → API
# (anon/public key — never put the service key here, this ships to the browser)
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
